# Scraper Configuration

`source_registry.json` defines approved discovery and crawl sources for the scraper/discovery layer.

The default runtime path is:

```text
config/scraper/source_registry.json
```

Override it with:

```bash
SOURCE_REGISTRY_PATH=/path/to/source_registry.json
```
