# Artifacts

Generated local outputs live here instead of the repository root.

```text
artifacts/
  scraping/      ad hoc scraper pipeline outputs
  test_output/   run_tests.sh and manual validation outputs
```

Most artifact files are ignored by git.
